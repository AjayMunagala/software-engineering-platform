# Dependency core conformance

Adapters and alternate neutral-core implementations run this reusable harness
before implementation-specific tests. It verifies deterministic output bytes,
immutable views, and the candidate contract's construction behavior.
