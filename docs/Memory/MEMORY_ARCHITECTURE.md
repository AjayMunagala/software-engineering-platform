# Memory Architecture — Future Design

Memory will store durable, attributable engineering knowledge: repository metadata, architectural decisions, validated investigations, user-approved conventions, and validation outcomes.

It must not silently convert unverified model output into fact. Records require scope, provenance, timestamp, confidence, retention policy, and a correction path. Repository-local knowledge must remain isolated unless a user explicitly authorizes sharing.
