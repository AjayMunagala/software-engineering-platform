# AI Pipeline

The AI pipeline is future work. It begins only after repository facts are available from the Repository Intelligence Engine.

```text
Authorized request -> Context selection -> Evidence package -> Model adapter
-> Structured proposal -> Evidence/uncertainty labeling -> Human review
```

The model may propose, summarize, or rank hypotheses. It does not become the authority on repository facts, permission, or validation.
